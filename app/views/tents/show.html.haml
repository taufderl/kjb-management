.row
  .col-md-9
    %h1.page-header
      Zelt
      = @tent.number
      %small
        = @tent.scouts.map {|s| s.first_name }.join ", "
  .col-md-3
    %br
    %br
    .btn-group
      = link_to 'Bearbeiten', edit_tent_path(@tent), class: "btn btn-primary"
      = link_to 'Übersicht', tents_path, class: "btn btn-success"
    
    
.row  
  %h3
    %ul
    - @tent.children.each do |child|
      %li= link_to child.first_name + ' ' + child.last_name, child
