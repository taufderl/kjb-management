%h1 Listing children

%table
  %thead
    %tr
      %th First name
      %th Last name
      %th Birthday
      %th
      %th
      %th

  %tbody
    - @children.each do |child|
      %tr
        %td= child.first_name
        %td= child.last_name
        %td= child.birthday
        %td= link_to 'Show', child
        %td= link_to 'Edit', edit_child_path(child)
        %td= link_to 'Destroy', child, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Child', new_child_path
